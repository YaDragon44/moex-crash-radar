# Data contract

`apartments.json` is the only runtime data source for the MVP UI.

The dashboard must not infer or generate missing business data. Use `null` or omit a field when the analyst has not confirmed it; the UI will show `⚠️ требует подтверждения` where applicable.
