# Apartment Radar

Static MVP dashboard for Moscow long-term rental decision support.

Entry point: `index.html`

Data contract: `data/apartments.json`

The UI renders only supplied analytical values. Unknown values remain marked as requiring confirmation. Direct listing URLs are never generated by the dashboard.
