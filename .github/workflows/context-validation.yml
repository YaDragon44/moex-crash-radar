name: Context Historical Validation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/moex_crash_radar/context_validation.py'
      - 'src/moex_crash_radar/historical_oil.py'
      - 'src/moex_crash_radar/context.py'
      - 'src/moex_crash_radar/rate_ofz.py'
      - 'src/moex_crash_radar/oil_rub.py'
      - 'src/moex_crash_radar/live_gate.py'
      - 'src/moex_crash_radar/calibration.py'
      - 'src/moex_crash_radar/history.py'
      - 'src/moex_crash_radar/backtest.py'
      - 'src/moex_crash_radar/moex.py'
      - 'scripts/run_context_historical_validation.py'
      - 'tests/test_context_validation.py'
      - 'tests/test_historical_oil.py'
      - 'pyproject.toml'
      - '.github/workflows/context-validation.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: pip install -e . pytest
      - name: Unit regression
        run: pytest
      - name: Run historical context validation
        run: python scripts/run_context_historical_validation.py
      - name: Upload validation artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: context-historical-validation
          path: artifacts/context_historical_validation.json
          if-no-files-found: warn
          retention-days: 14
